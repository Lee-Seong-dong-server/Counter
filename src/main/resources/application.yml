spring:
  datasource:
    url: jdbc:mysql://localhost:3306/counter
    username: member
    password: 1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  port: 8080
